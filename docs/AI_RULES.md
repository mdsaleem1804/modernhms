# AI Agent Rules

* Do NOT execute terminal commands (npm run build, npm install, etc.)
* Do NOT start or restart servers
* Only modify code files
* Keep changes minimal and focused
* Do not modify unrelated files
